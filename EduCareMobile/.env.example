# Environment Configuration
NODE_ENV=development

# API Configuration
# Choose the appropriate URL based on your setup:

# For Android Emulator (default):
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:5000/api
EXPO_PUBLIC_SERVER_URL=http://10.0.2.2:5000

# For iOS Simulator:
# EXPO_PUBLIC_API_BASE_URL=http://localhost:5000/api
# EXPO_PUBLIC_SERVER_URL=http://localhost:5000

# For Physical Device:
# Replace YOUR_COMPUTER_IP with your actual IP address
# You can find it by running: ipconfig (Windows) or ifconfig (Mac/Linux)
# EXPO_PUBLIC_API_BASE_URL=http://YOUR_COMPUTER_IP:5000/api
# EXPO_PUBLIC_SERVER_URL=http://YOUR_COMPUTER_IP:5000

# App Configuration
EXPO_PUBLIC_APP_NAME=EduCare Mobile
EXPO_PUBLIC_APP_VERSION=1.0.0

# Debug Configuration
EXPO_PUBLIC_DEBUG_MODE=true
EXPO_PUBLIC_ENABLE_LOGGING=true

# Instructions:
# 1. Copy this file to .env
# 2. Update the API URLs based on your setup:
#    - Android Emulator: Use 10.0.2.2 (already set)
#    - iOS Simulator: Use localhost or 127.0.0.1
#    - Physical Device: Use your computer's actual IP address
# 3. To find your IP address:
#    - Windows: Open cmd and run "ipconfig"
#    - Mac/Linux: Open terminal and run "ifconfig" or "ip addr"
#    - Look for your local network IP (usually starts with 192.168.x.x)
